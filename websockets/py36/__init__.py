# This package contains code using the PEP 525 asynchronous generator
# syntax introduced in Python 3.6.  It cannot be imported on Python < 3.6
# because it will raise syntax errors, for example:
# > SyntaxError: 'yield' inside async function
